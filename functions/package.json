{
  "name": "runiac-complete-run-functions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "lib/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test lib/test/whiteboxProgressionCases.test.js",
    "feed:fixtures:emulator": "npm run build && GCLOUD_PROJECT=demo-runiac-feed FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 FUNCTIONS_EMULATOR_HOST=127.0.0.1:5001 FIREBASE_STORAGE_EMULATOR_HOST=127.0.0.1:9199 node lib/src/feed/fixtures/cli.js",
    "leaderboard:seed": "npm run build && node lib/src/leaderboard/seedLeaderboardMockData.js",
    "leaderboard:seed:emulator": "npm run build && firebase emulators:exec --only functions,firestore --project runiac-functions-test \"node lib/src/leaderboard/seedLeaderboardMockData.js --project runiac-functions-test --period 2026-07 --run-id leaderboard-je-emulator-local --region-id jurong-east --users-per-region 100 --lifecycle\""
  },
  "dependencies": {
    "@langchain/core": "1.2.2",
    "@langchain/langgraph": "1.4.7",
    "@langchain/openai": "1.5.5",
    "firebase-admin": "13.6.0",
    "firebase-functions": "6.4.0"
  },
  "devDependencies": {
    "firebase-tools": "14.27.0",
    "typescript": "5.9.3",
    "@types/node": "22.19.0"
  }
}
